/**
 * Configuration exports
 * Central export point for all configuration modules
 */

export * from './brand';

// Re-export brand config as default for convenience
export { BRAND_CONFIG as default } from './brand';